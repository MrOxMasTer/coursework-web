import { LoginFormSection } from '@/pages/login/ui/LoginFormSection/LoginFormSection';

export default async function LoginPage() {
  return (
    <main>
      <LoginFormSection />
    </main>
  );
}

// TODO: next-auth -> credential provider
